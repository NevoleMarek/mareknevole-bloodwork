You are given an existing bloodwork vocabulary and a newly extracted bloodwork reading.

EXISTING VOCABULARY:
{{VOCABULARY}}

NEWLY EXTRACTED READING:
{{EXTRACTED_READING}}

Your task:
1. For each measurement in the extracted reading, find the matching vocabulary entry by test name (use fuzzy/case-insensitive matching — e.g. "Hba1c" matches "HbA1c").
2. For measurements with no match, create a new VocabularyEntry: derive a snake_case "key" from the label, preserve the unit and reference range from the extracted data.
3. For all measurements (matched or new), produce a normalized measurement using the vocabulary key.

Return ONLY a raw JSON object — no markdown, no explanation, no code fences.

The JSON must match this exact structure:

{
  "date": "YYYY-MM-DD",
  "newVocabularyEntries": [
    {
      "key": "snake_case_key",
      "label": "Display Name",
      "unit": "unit string",
      "referenceRange": { "min": 0.0, "max": 100.0 }
    }
  ],
  "normalizedMeasurements": [
    {
      "vocabularyKey": "snake_case_key",
      "value": 98.0,
      "unit": "mg/dL",
      "status": "normal"
    }
  ]
}

Rules:
- "date": copy from the extracted reading
- "newVocabularyEntries": only entries that do not already exist in the vocabulary; empty array if all matched
- "normalizedMeasurements": one entry per measurement in the extracted reading, in the same order
- If a reference range bound is null in the extracted data, use 0 for min or a reasonable upper bound for max based on context; prefer to use the existing vocabulary range when available
- "status": copy from the extracted measurement
